interface:
  display_name: "Issue Search"
  short_description: "Search a target GitHub repo issues by keyword, topic, or label"
  default_prompt: "Use $issue-search to search a target GitHub repo issues by keyword, topic, or label. Resolve the repo and search terms in the main thread, then delegate the gh issue search and result synthesis to a fresh Codex subagent using model gpt-5.4 with reasoning xhigh."
