interface:
  display_name: "HFT Playground Snap Factor Write"
  short_description: "当用户要用 Playground 编写/新增/修改【snap 族因子】——纯基于交易所 L2 快照(SnapBook / Context::GetSnap)的因子(如 snap-to-snap 变化量、当前帧 imbalance/spread/microprice)时使用；按 snap 自身时间每帧采样、同时记录 exchange_ts 与 local_ts。开始写代码前必须先阅读 /home/cken/hft_projects/HftKnowledge/research_docs/data.md(SnapBook / GetSnap / 双 book 章节)，并以文档口径为准。**若写的是基于 order/trade 重构 OrderBook 的因子，请改用 `hft-playground-factor-write`。**"
  default_prompt: "Use $hft-playground-snap-factor-write when the user request matches this skill: 当用户要用 Playground 编写/新增/修改【snap 族因子】——纯基于交易所 L2 快照(SnapBook / Context::GetSnap)的因子(如 snap-to-snap 变化量、当前帧 imbalance/spread/microprice)时使用；按 snap 自身时间每帧采样、同时记录 exchange_ts 与 local_ts。开始写代码前必须先阅读 /home/cken/hft_projects/HftKnowledge/research_docs/data.md(SnapBook / GetSnap / 双 book 章节)，并以文档口径为准。**若写的是基于 order/trade 重构 OrderBook 的因子，请改用 `hft-playground-factor-write`。** Follow the skill workflow exactly and preserve any required gates, checks, and external-command safety rules."
