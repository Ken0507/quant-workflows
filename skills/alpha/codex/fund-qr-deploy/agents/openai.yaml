interface:
  display_name: "fund-qr 新疆部署"
  short_description: "将 fund-qr 以 dev 或 release 模式部署到新疆投研机"
  default_prompt: "使用 $fund-qr-deploy 部署 fund-qr；默认 dev 模式只同步源码并测试，release 模式必须先通过 tag、VERSION 和 findata>=0.13 预检，再构建 wheel、安装公共 py311 环境并完成真实取数 smoke。"
